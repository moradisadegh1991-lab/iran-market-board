# CLAUDE.md — راهنمای ادامه کار در Claude Code

‏این پروژه در یک نشست طولانی با Claude (چت) ساخته شده. این فایل خلاصه‌ای است تا Claude Code
بدون این‌که تاریخچه گفت‌وگو را ببیند، بداند پروژه کجاست، چه تصمیم‌هایی عمدی بوده‌اند و کجا
ادامه بدهد. **قبل از هر تغییری این فایل را کامل بخوان.**

‏تاریخ این خروجی: ۱۴۰۵/۰۶/۲۹ (۲۰ سپتامبر ۲۰۲۶). همه‌چیز که این‌جا «سبز» گفته شده، همین امروز
واقعاً اجرا و تأیید شده — نه از روی حافظه.

---

## ۱) این پروژه چیست

‏«تابلوی بازار ایران» (Iran Market Board): یک داشبورد Next.js روی Vercel + ربات تلگرام برای
بازار طلا/ارز/کریپتو/بورس ایران، به‌همراه یک اپ اندرویدی آفلاین (Capacitor) که همان داده را
نمایش می‌دهد و چند قابلیت کاملاً روی گوشی (بدون سرور) دارد.

‏دو زیرپروژه مستقل با دو `package.json` جدا:

```
/                    ← اپ اصلی Next.js (این را روی Vercel دیپلوی می‌کنید)
/android-app/        ← پوسته Capacitor اندروید (این را جدا build می‌کنید)
```

## ۲) نقشه پوشه‌ها

```
app/                 صفحات: / (نمای کلی) · scenarios · simulator · live (معامله برخط)
                     · swing (نوسان‌گیری) · charts · risk · stocks · crypto · portfolio · bot
app/api/             snapshot · diag · ingest · chart · simulate · paper/{,tick}
                     · swing (backtest/portfolio/scan) · swing-live/{,tick} · learning
                     · holdings · cron/* · telegram/{webhook,setup,broadcast}
lib/sources/         tgju · goldapi · nobitex · brsapi · coingecko · history · news · cache
lib/engine/          stats · risk · crypto · tse · portfolio · scenario · simulator · live
                     · learning · swing · swing-portfolio · swing-scan · holdings-analysis
lib/telegram/        api · format · handler · paper
lib/                 snapshot · series · history · simulate · paper · learning · intraday
                     · holdings · jalali · swing-history · swing-live · store · auth · num · http
components/          Shell · SnapshotProvider · ui · TradeEntry · EquityChart · PriceChart
                     · Sparkline · RollingNumber · HoldingsPanel
components/views/    Overview · Scenarios · Simulator · Live · Swing · Charts · Risk · Stocks
                     · Crypto · Portfolio · Bot
scripts/             تست‌های آفلاین همه با tsx اجرا می‌شوند — بخش ۵ را ببین
.github/workflows/   paper-tick.yml (ضربان‌ساز معامله برخط) · build-android.yml (ساخت APK)
middleware.ts        CORS فقط روی مسیرهای فقط-خواندنی، برای اپ اندرویدی

android-app/www/     کل اپ اندروید: یک index.html خودبسند + game-engine.js + sms-parser.js
android-app/native-plugin/   پلاگین Java خواندن پیامک (⚠️ تست‌نشده — بخش ۷ را ببین)
android-app/scripts/ wire-native-plugin.mjs (خودکار وصل‌کردن پلاگین) + تست‌هایش
```

## ۳) دیپلوی و حساب‌ها

- ‏Vercel team: `moradisadeghs-projects` (`team_MqnNkhzVLyfZxaVcgBELmPXX`)
- ‏GitHub: `moradisadegh1991-lab`
- ‏ریپوی گیت‌هاب فعلاً **خصوصی** است — این محدودیت واقعی دارد، بخش ۸ را ببین.
- ‏دیپلوی با push به Vercel (خودکار) یا zip دستی؛ کاربر معمولاً از طریق Termux/Claude Code
  روی گوشی خودش (Galaxy Z Fold 5) کار می‌کند، نه از دسکتاپ.

### متغیرهای محیطی لازم (Vercel)
‏لیست کامل و توضیح هرکدام در `.env.example` است. خلاصه مهم‌ترین‌ها:

| متغیر | برای چی |
|---|---|
| `ADMIN_SECRET`, `CRON_SECRET` | دو رمز جدا؛ `ADMIN_SECRET` هم به‌صورت query param (`?secret=`) هم هدر (`x-admin-secret`) کار می‌کند، `CRON_SECRET` فقط با هدر `Authorization: Bearer` |
| `BRSAPI_KEY` | فقط پکیج رایگان `Market_CGCC` را دارند (Index + AllSymbols + Gold_Currency)؛ بورس کالا/صندوق/عرضه اولیه نیاز به پکیج پولی دارند — بخش ۹ |
| `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET` | ربات `@MyKasbai_bot` |
| `UPSTASH_REDIS_REST_URL/TOKEN` یا `KV_REST_API_URL/TOKEN` | بدون این، storeMode روی `memory` می‌افتد و همه‌چیز بین cold start پاک می‌شود |

### متغیرهای محیطی اپ اندروید
‏هیچ‌کدام! اپ اندروید کاملاً بدون سرور بخش‌هایی دارد (دارایی‌ها، بازی، هزینه‌ها) که فقط
`localStorage` گوشی را می‌خوانند. تنها چیزی که لازم است `BOARD_URL` است که با
`node set-url.mjs https://...` داخل `www/index.html` نوشته می‌شود (بخش ۷).

## ۴) قواعدی که نباید بدون خواندن این‌جا شکسته شوند

‏این‌ها هرکدام یک باگ واقعی بودند که پیدا و رفع شدند. اگر کدی می‌نویسی که این قواعد را
دور می‌زند، احتمالاً همان باگ را دوباره می‌سازی:

1. **‏واحد پول همیشه صریح باشد.** بیشتر مبالغ داخلی به **ریال** ذخیره می‌شوند (نه تومان)؛
   تبدیل به تومان فقط موقع نمایش. برخی منابع (BrsApi Gold_Currency، پیامک بانکی) به‌صورت
   دیگری می‌دهند — قبل از جمع‌زدن دو عدد از منبع متفاوت، واحدشان را چک کن.
2. **‏قیمت دلاری با تومانی قاطی نشود.** آیتم‌های تابلو (`snap.live.items`) یک فیلد `unit`
   دارند (`'toman' | 'usd' | 'point'`)؛ قبلاً بیت‌کوین دلاری به‌جای تومانی جمع زده می‌شد و
   ارزش دارایی ۶٬۳۴۳ تومان به‌جای ۱٫۱۹ میلیارد نشان می‌داد.
3. **‏جهت تراکنش هرگز حدس زده نشود.** اگر پیامک بانکی فعل برداشت/واریز ندارد، جهتش
   `typeUnknown: true` علامت می‌خورد و از کاربر پرسیده می‌شود — نه این‌که پیش‌فرض واریزی
   گرفته شود (این باگ در ExpenseTracker اصلی بود: خرید کارتی بدون فعل، به‌عنوان درآمد
   ثبت می‌شد).
4. **‏نگاه به آینده (lookahead) در تحلیل ممنوع.** تحلیل زمان خرید دارایی‌ها فقط از قیمت‌های
   **تا روز خرید** استفاده می‌کند؛ غربال خودکار نوسان‌گیری با نیمه اول تاریخچه انتخاب و با
   نیمه دوم (دیده‌نشده) سنجیده می‌شود. `scripts/holdings-test.ts` و `scripts/swing-scan-test.ts`
   این را قفل کرده‌اند.
5. **‏Backtest ≠ زنده.** حداقل نگه‌داری/فاصله ورود مجدد موتور معامله‌گر برای بک‌تست چندماهه
   نوشته شده؛ روی جلسه برخط کوتاه با `scaleParamsForSession()` کوچک می‌شود، وگرنه معامله
   دوم اصلاً ممکن نیست.
6. **‏هیچ درخواست شبکه‌ای در اپ اندروید نباید سرور را عوض کند.** `middleware.ts` فقط
   مسیرهای فقط-خواندنی (`snapshot`, `chart`, `swing`, `simulate`) را CORS باز کرده؛
   `paper`, `holdings`, `telegram`, `ingest` عمداً باز نشده‌اند.
7. **‏دارایی‌های شخصی کاربر هرگز به سرور نمی‌روند.** دارایی‌های اپ اندروید، هزینه‌ها،
   کارت‌ها و حساب‌ها همه فقط `localStorage` هستند. تنها استثنا: زبانه «مشاور» در بخش
   هزینه‌ها که با کلید API خود کاربر به OpenRouter وصل می‌شود — و فقط خلاصه اعداد را
   می‌فرستد، نه متن پیامک.
8. **‏گیت‌هاب اکشن‌ها روی ریپوی خصوصی ماهی ۲٬۰۰۰ دقیقه رایگان دارد، روی عمومی نامحدود.**
   `paper-tick.yml` قبلاً این را در دو روز تمام می‌کرد (بخش ۸).

## ۵) چک‌لیست تأیید قبل از هر تحویل

‏همه این‌ها همین امروز اجرا و سبز شدند. قبل از commit کردن هر تغییری، حداقل تست‌های مربوط
به بخشی که دست خورده را اجرا کن:

```bash
# اپ اصلی (از ریشه پروژه)
npx tsc --noEmit
npm run smoke && npm run smoke:sim
npm run test:jalali && npm run test:holdings
npm run test:swingpf && npm run test:swingscan && npm run test:swinghist
npm run test:activity
npx tsx scripts/sim-regression.ts      # هش‌های بک‌تست؛ باید ثابت بمانند
npx tsx scripts/swing-validate.ts
npx tsx scripts/learn-live-test.ts
npx tsx scripts/swing-live-test.ts
npx tsx scripts/walk-forward-test.ts   # کند است، چند دقیقه طول می‌کشد

# اپ اندروید (از android-app/)
node scripts/sms-parser-test.mjs
node scripts/wire-plugin-test.mjs
```

‏برای دیدن UI واقعی (نه فقط تست منطق)، از skill مربوط به frontend-design استفاده کن و با
Playwright + یک mock برای fetch اسکرین‌شات بگیر — همان روشی که در کل این پروژه استفاده شد.
هیچ صفحه‌ای را بدون دیدنش در مرورگر (حتی هدلس) تحویل نده.

## ۶) نکته‌های خاص محیط (اگر در محیطی مثل من کار می‌کنی)

‏اگر Claude Code هم در یک sandbox بدون حافظه پایدار بین دستورها کار می‌کند (مثل من)، این
الگوها لازم بودند:

- **‏پروسه‌های پس‌زمینه بین دو فراخوانی جدا از بین می‌روند.** اگر سرور را در یک دستور بالا
  بیاوری و در دستور بعدی تست کنی، سرور دیگر وجود ندارد. `server start + sleep + test` را
  همیشه در **یک** دستور بنویس.
- ‏برای دیدن یک صفحه واقعی، یک HTTP server ساده برای `android-app/www/` و یک `next start`
  با `NODE_OPTIONS="--import mock-fetch.mjs"` (mock کامل fetch با داده واقعی‌نما) لازم است.
  چنین mock فایلی دیگر در پروژه نگه داشته نشده (فقط برای تست موقت بود)؛ در صورت نیاز
  دوباره بساز، ولی مقادیر واقعی (نه فرضی) بگذار — چند بار در این پروژه، مقدار فرضی در
  mock باعث شد یک باگ واقعی دیده نشود.
- ‏Android SDK در این محیط نصب نیست، پس پلاگین Java هرگز کامپایل/تست نشده — بخش ۷.

## ۷) ⚠️ چیزی که واقعاً تست نشده: پلاگین بومی پیامک

‏`android-app/native-plugin/java/SmsReaderPlugin.java` (۱۱۴ خط) — فقط پیامک خام را می‌خواند،
هیچ منطقی در آن نیست (منطق در `sms-parser.js` است که کاملاً تست شده). ولی خودِ این فایل Java
**هیچ‌وقت کامپایل یا روی گوشی واقعی اجرا نشده**، چون Android SDK در هیچ‌کدام از محیط‌های من
نبوده.

‏نصبش خودکار است: `npm run wire-plugin` (کپی فایل + ثبت در `MainActivity.java` + مجوزها).
این اسکریپت با هر دو شکل واقعی `MainActivity.java` که Capacitor می‌سازد تست شده
(`test:wireplugin`)، ولی خودِ کد Java نه.

‏**اگر Claude Code به Android SDK دسترسی دارد، اولین کاری که باید بکند این است که این
پلاگین را واقعاً کامپایل و روی یک گوشی یا شبیه‌ساز واقعی با پیامک بانکی واقعی تست کند.**

## ۸) وضعیت گیت‌هاب اکشن‌ها (مهم، همین چند روز پیش رفع شد)

‏نسخه قدیمی `paper-tick.yml` هر ساعت ۵۵ دقیقه runner را بیدار نگه می‌داشت (~۴۰٬۰۰۰ دقیقه در
ماه). روی ریپوی خصوصی کاربر، سهمیه رایگان ۲٬۰۰۰ دقیقه‌ای گیت‌هاب را در کمتر از دو روز تمام
کرد — و با تمام‌شدن سهمیه، **همه** workflowهای ریپو (از جمله بیلد اندروید) تا ریست ماهانه
متوقف می‌مانند.

‏نسخه فعلی: هر اجرا فقط یک تیک می‌زند (~۱ دقیقه)، ساعتی، ~۷۲۰ دقیقه در ماه. روش پیشنهادی
برای تیک مکرر، یک cron رایگان **بیرون از گیت‌هاب** است (cron-job.org) — جزئیات کامل در
`README.md`، بخش «معامله برخط».

‏اگر الان که این را می‌خوانی سهمیه هنوز صفر است، هیچ workflowای اجرا نمی‌شود تا کاربر ریپو
را عمومی کند یا صبر کند — این را با `gh api /user` یا از طریق کاربر تأیید کن قبل از این‌که
فرض کنی CI کار می‌کند.

## ۹) کارهای باقی‌مانده که قبلاً به کاربر گفته شده و منتظر تصمیم اوست

‏این‌ها **عمداً** ساخته نشده‌اند چون یا به پول نیاز دارند یا به چیزی که در محیط من نبود؛
وعده «به‌زودی می‌سازمش» به کاربر داده نشده، پس اگر شروع می‌کنی صریح بگو داری شروع می‌کنی:

- ‏**بورس کالا، صندوق‌های درآمد ثابت، عرضه اولیه** — نیاز به پکیج پولی BrsApi
  (`IME_*`, `TSETMC_Nav`, `CODAL_Announcement`) که کاربر هنوز نخریده.
- ‏**معاملات فیوچرز/اهرمی** — عمداً نساختم چون ریسک نقدشدگی کامل دارد و باید با سقف اهرم
  و لیکویید شبیه‌سازی‌شده طراحی شود؛ منتظر تأیید صریح کاربر.
- ‏**بخش‌های ExpenseTracker که منتقل نشدند:** اعلان اندروید با دکمه انتخاب دسته مستقیم
  از نوتیفیکیشن (`NotificationHelper`/`CategoryActionReceiver` در اپ اصلی) — به کد بومی
  بیشتری نیاز دارد که باز هم تست‌نشده می‌ماند؛ در عوض تراکنش‌های تازه در زبانه «نیازمند
  تأیید» دیده می‌شوند.

‏همچنین **چهار باگ واقعی در اپ اصلی ExpenseTracker کاربر** (نه در این پورت) پیدا شده و به او
گزارش شده، ولی خودِ آن اپ اصلاح نشده — اگر روزی از تو خواسته شد به آن پروژه هم سر بزنی،
باگ‌ها در تاریخچه commit همین پورت (`sms-parser.js`) با کامنت توضیح داده شده‌اند.

## ۱۰) سبک کدنویسی و انتظارات کاربر

- ‏کاربر فارسی می‌خواند؛ توضیح‌ها و پیام‌های UI فارسی، کد و نام متغیر انگلیسی.
- ‏مستقیم و بدون مقدمه؛ از قضاوت‌های اغراق‌آمیز درباره کیفیت خودش پرهیز کن.
- ‏هر ادعای «درست کار می‌کند» باید با یک تست واقعی (نه فقط خواندن کد) پشتیبانی شود —
  در طول این پروژه چند بار کدی که «باید درست باشد» با تست واقعی نادرست از آب درآمد
  (نمونه‌ها: قیمت USDT با واحد اشتباه، حباب‌های تاریخ نگاه‌به‌آینده، رگرسیون سهمیه دقیقه).
- ‏قبل از افزودن هر قابلیت جدید، اگر مکانی برای پرسیدن دارد (مثل انتخاب معماری)، بپرس؛
  ولی اگر پاسخ روشن است، مستقیم بساز و تست کن — کاربر «تست کن، بعد تحویل بده» را
  به‌کرات درخواست کرده.
